# GitHub Stats

用于生成个人 GitHub 概览和语言统计 SVG。脚本读取 GitHub 数据，通过仓库中的模板生成图片。

## 使用

安装 `requirements.txt` 中的依赖，并配置 `ACCESS_TOKEN` 和 `GITHUB_ACTOR` 环境变量，然后运行：

```bash
python github_stats.py
python generate_images.py
```
